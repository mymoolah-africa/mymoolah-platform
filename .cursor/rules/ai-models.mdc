---
description: AI model selection for MyMoolah services and RAG system architecture
globs: ["**/services/**/*.{js,ts}", "**/*rag*.js", "**/*kyc*.js"]
alwaysApply: false
---

# AI Model Selection

Do not change model assignments without explicit André approval.

## Current Assignments

- `ragService.js`: `gpt-4o-mini` for support chat cost control.
- `feedbackService.js`: `gpt-4o-mini`.
- `googleReviewService.js`: `gpt-4o-mini`.
- `codebaseSweepService.js`: `gpt-4o-mini`.
- KYC/OCR identity verification: keep `gpt-4o` unless André approves a change.

## RAG Rules

- Personal responses are never cached (POPIA).
- Cost path: Redis cache -> direct KB hit -> `gpt-4o-mini` -> self-learning.
- Run `npm run embed:kb` after adding active KB entries.
- Unknown questions may be saved inactive for admin review.

## Legacy AI

Do not call legacy AI services unless explicitly reviving them. Prefer current RAG flow and check `docs/AGENT_HANDOVER.md` for latest status.
