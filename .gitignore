node_modules/
.env
.env.*
backups/
/certs/
reports/
*.backup
*.backup2
.DS_Store

# Generated reconciliation files (ignore all generated recon files)
*_recon_*.csv
flash_recon_*.csv
mobilemart_recon_*.csv
recon_*.csv
FULCRUM.MERCHANT.*.txt
easy*.???

# Local-only
.env
.gcp-sa.json
uploads/
idempotencyKey

# SSH public keys from partners — stays local (repo is public).
# The keys themselves are not secret, but keeping them out of a public repo
# keeps our partner-key inventory out of the open for supply-chain hygiene.
keys/

# Node modules
mymoolah-wallet-frontend/node_modules/

# Vite cache/timestamp files (auto-generated, safe to delete)
vite.config.*.timestamp-*
*.timestamp-*.mjs

# Frontend build output
portal/admin/frontend/dist/
mymoolah-wallet-frontend/dist/

# Binary files
cloud-sql-proxy
bin/cloud-sql-proxy
*.exe
*.dmg
*.pkg
actions
chemaname = public

# EasyPay — contains SessionToken, do not commit
docs/COPY_PASTE_THEODORE_EMAIL.txt
# EasyPay UAT test PINs (generated locally; do not commit — enumerateable credentials)
docs/integrations/easypay_test_pins.csv

# Docker container ID artifacts (created by redis/proxy startup)
Running
[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f]
